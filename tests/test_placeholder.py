"""
Placeholder test module.
Real tests will be added alongside src/ utilities as they are implemented.
See CLAUDE.md: tests live in tests/ mirroring the src/ structure.
"""


def test_project_is_importable() -> None:
    """Smoke test — ensures the test suite can be collected by pytest."""
    assert True
